# Dark theme so the app chrome matches the dark-background charts.
[theme]
base = "dark"
primaryColor = "#7aa2f7"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#161b22"
textColor = "#fafafa"

[server]
headless = true
